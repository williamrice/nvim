-- VSCode-specific options

-- Note: Clipboard is configured in shared/options.lua with vim.opt.clipboard = "unnamedplus"
-- This works for both native Neovim and VSCode

-- Note: Most UI options like number, relativenumber, cursorline, signcolumn, etc
-- are controlled by VSCode settings and are forced by the extension.
-- See VSCode settings.json to configure:
-- - "editor.lineNumbers"
-- - "editor.cursorStyle"
-- - "editor.scrollBeyondLastLine"
-- - etc
